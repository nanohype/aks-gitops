# Tempo - Production overrides
# Production: longer retention, larger storage

serviceAccount:
  labels:
    azure.workload.identity/use: "true"
  annotations:
    azure.workload.identity/client-id: "91b2056d-bdb4-4a87-b5e8-615b57fbb291"

tempo:
  retention: 720h

persistence:
  size: 100Gi
