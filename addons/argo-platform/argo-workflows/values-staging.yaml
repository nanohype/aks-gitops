# Argo Workflows - Staging overrides

server:
  serviceAccount:
    labels:
      azure.workload.identity/use: "true"
    annotations:
      azure.workload.identity/client-id: "00000000-0000-0000-0000-000000000000"

artifactRepository:
  azure:
    endpoint: https://aksstagingartifacts.blob.core.windows.net
    container: argo-artifacts
